# 开发环境配置
server:
  port: 8080

spring:
  # 数据库配置（如果需要的话）
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  h2:
    console:
      enabled: true
      path: /h2-console

# 开发环境日志级别
logging:
  level:
    root: INFO
    com.example.cursorquitterweb: DEBUG 