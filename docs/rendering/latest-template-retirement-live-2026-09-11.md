# 无引用旧模板实际清理结果

本地网站后端加载 `df26243`，进程 82667。启动后的既有定时清理任务成功删除 7 条无引用旧内容，数据库内容记录由 22 条降至 15 条。8 个正式模板及其当前内容标识不变；剩余 7 条旧内容因项目或任务明确引用继续保留，不在模板库作为新增模板展示。

清理前后对所有用户项目、项目照片关联和用户素材记录分别按主键排序并计算完整 JSON 的 SHA-256，三组数量与哈希全部一致。证据只记录对比结果，不保存用户照片地址或身份。没有修改用户项目、照片或生成视频。

121 个 Cloudflare Images 标识和 7 个 Cloudflare Stream 标识已登记到延迟清理队列；本轮云文件删除数为 0。队列至少等待 24 小时，并在每个文件删除前再次核对实际引用。排队不代表这些文件最终全部能删，也不代表已释放对应云存储。共享 SDK 和共享资源没有进入本轮删除范围。

验证包括前序定向 Java 编译、11 种 SQLite 淘汰场景、3 项实际 SQL 发布/引用、6 项发布服务/原生场景和前端 9 项项目恢复测试，以及本次实际 D1 批处理结果。服务日志无 ERROR、无清理失败。没有运行视频或 SSIM；本次不改变渲染执行语义。

同名 JSON 保存聚合证据。该结果证明本轮无引用内容清理已落地，不能据此宣称全部旧依赖已删除或 nativeEngine 迁移整体完成。
