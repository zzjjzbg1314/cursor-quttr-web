# 原生全局效果接入验证（2026-09-11）

## 实现范围

独立 filter/video_effect 采用已核实的官方 enT/enb→eng→enh 轨道契约：保留目标放置区间、资源读取区间、原始 offset、素材参数、轨道索引与内部层号。原生运行包使用显式 globalEffectPolicy=original_tracks_v1，必须逐项接管全部声明的全局效果。

研究端保留 nativeGlobalSource，并停止按零强度直接删除全局效果。网站后端保留接管策略，校验原始时钟、参数、身份和已交付资源。前端完整场景规划合并独立效果轨道，并在原始绑定编译通过后排除对应后处理执行；原始场景保留用于审计。最小依赖编译按相同归属排除重复后处理依赖。未知关键帧仍阻塞，未补通所有调色关键帧。

## 实际验证

- 验证任务：0827-global-native-20260911-attempt-1。
- 使用原始 0827 素材和已授权的默认字体副本，保留 Green Orange 与尾部效果；两项全局效果的原生资源绑定均进入同一时间轴。未调用旧 Mac 自研渲染矩阵。
- 最小运行归档 1,677,466 字节，259 个声明文件；不保留完整运行 ZIP。
- 基础输出 completed，1080×1920、30 fps、1003 帧，视频流 33.433333 秒。渲染记录 25.08252 秒，总计 32.23471 秒。
- semanticIntegrity=exact、编码 1 次、中间视频 0；完成后浏览器 OPFS 文件 0，Writer/raw 残留 0。
- SSIM 基础 7 点平均 0.945103、最低 0.828620，MAE 平均 7.9929；关键 3 点平均 0.961313、最低 0.910789。
- 现有平均验收规则通过，未降低阈值；不是所有采样点均超过 0.90，也不是 1003 帧逐帧比较。

## 检查与边界

前端 22 项测试及 TypeScript 检查通过；研究端原始绑定 3 项、场景服务 50 项、最小依赖 13 项通过；网站后端契约 15 项、运行依赖交付 7 项通过。修改的 Java 类编译通过。最小依赖测试首轮缺少测试运行时 LuaJ，补齐既有依赖后通过；没有借此重试视频渲染。

本轮真实渲染通过客户前端原有 renderBrowserMusicMv 和同一原生执行器，验证输入由本地静态页面提供。网站后端已完成代码与契约测试，但本轮新包尚未同步发布或经过网站真实取包验证；已上架版本指针未改变。旧 scene 中的近似后处理元数据保留用于审计，此次归属策略排除了这两项对应后处理，不把旧近似声明直接改成精确声明。

整体迁移仍未完成：后续需验证网站交付新包、客户编辑与预览、剩余模板，并在覆盖充分后退役重复实现。新全局关键帧、其他算法和资源组合仍需严格能力检查。
