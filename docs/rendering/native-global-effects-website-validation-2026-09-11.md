# 0827 原生全局效果网站上线验证（2026-09-11）

网站当前版本已切换至 `tplver_75fb3e1e992c46bab66f3ef694cbdaca`，本地版本 `tplver_13af90c738146376`，归档 `intake_9d8c311775d3b83aabcadb1ddedf150e`。发布状态为 published/completed，管理 API 的 currentVersionId 与同步日志一致。

实际从 8080 网站候选接口取得场景和资源，确认 original_tracks_v1 与两项 nativeGlobalSource 完整交付。浏览器只使用网站交付资源；参考视频在本地验证页仅提供比较用音频，不作为客户页面公开素材。

网站输出 720×1280、30 fps、1003 帧，视频流 33.433333 秒。渲染记录 26.20583 秒，含准备总计 44.023495 秒；编码一次、中间视频零，OPFS 文件及 Writer/raw 残留均为零。完整输出哈希见 JSON。

SSIM 基础 7 点平均 0.945216，最低 0.828474，MAE 平均 8.0138；关键 3 点平均 0.961669，最低 0.908782。现有平均验收规则通过；没有降低阈值，不代表全部采样点超过 0.90，也不是逐帧验收。此份网站输出的真实证据已登记后端，再通过正常发布入口上线。

本轮没有再次修改运行代码。验证页类型检查通过，临时页面和公共验证文件已清理，独立证据保留。8080/8082 已加载前轮提交的新代码。

本轮证明网站交付与单次模板输出。该新版本的客户编辑导出、长歌曲跨周期导出尚未验收，不能引用先前单次输出记录宣称这两项完成。整体迁移目标仍在进行，未退役重复实现。
