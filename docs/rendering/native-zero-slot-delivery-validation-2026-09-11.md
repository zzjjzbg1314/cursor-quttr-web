# 已验收零照片槽位场景交付修复

研究端已验收入库与归档同步预检现在保留明确的零照片槽位契约。共享槽位解析器要求slots确实为数组，逐项检查对象、标识和重复项；缺失/null/畸形不能被清洗成空数组。空槽位场景还需具有视觉图层。场景槽位与版本槽位必须一致，两者均明确为空时允许继续。保留照片槽位资产校验、源文件哈希及原有能力/验收门禁。此解析器不是能力验证器，不据此宣称未知图层可执行。

网站端TemplatePromotionRequest允许显式空slots数组，但默认值及null不再自动转换为空数组，仍由@NotNull拒绝缺失契约；最多200项限制不变。未经验证的关键帧场景仍在保存前拒绝。没有添加虚假照片，也没有改动原始图层、效果、SDK或最小依赖策略。

检查：研究端槽位解析3项、实际入库2项（包括新增零槽位及旧照片归档回归）、同步服务32项；网站端DTO2项及关键帧服务契约2项；前端照片槽位6项、贴纸规划3项，共50项。Java编译与TypeScript检查通过。旧classpath缺spring-tx/spring-jdbc/H2、旧已编译测试缺新增方法等测试环境问题已补齐；未将容器启动失败误报为测试通过。

最终本地基础渲染使用已有纯贴纸夹具：1080×1920、30fps、16.133333秒、484帧；completed，渲染记录5.14384秒，运行时合计10.86397秒，exact、编码1、中间视频0、OPFS0，无Writer残留。成片1141594字节，ffprobe核对H.264与帧数；SHA256 e28eea885928434e16ef4b1614e0c3b107f13e942364cd97c33c489e55db01dc。单worker、15分钟硬超时。原始贴纸依赖哈希匹配；音轨取此前基础成片，仅是测试输入，不作为官方参考或SSIM基准。

边界：测试验证已验收会话的入库服务、同步服务回归、网站请求边界和本地零槽位执行；未实际向云端创建或上架模板，未进行零槽位场景的完整D1/R2发布往返。旧Mac照片模板验收和未经该归档流程的latest-draft入口仍保留原照片要求，本次没有批量删除旧路径检查。未运行SSIM，独立夹具没有独立官方参考视频；0908未重跑。8080/8082运行服务未替换，本轮编译验证在临时目录。临时前端页面、资源和标签已清理，源码本地提交不等于部署或整体迁移完成。
