# 场景花字交付契约修复

0915 (9) 已通过浏览器验收，但后端将 text_style 一律限定为 effectStyle.json，拒绝 InfoSticker 的 content.json + prefab 原始入口。

按同一资源目录校验两类交付清单，拒绝缺失配置、跨资源 prefab 和混合入口。配置内容、依赖闭包仍由编译器及浏览器核验；此处不改写运行资源或渲染计划。

验证：117 项测试通过（契约 19、场景能力 2、运行包 10、模板目录 86）。实际已验收运行包的 nativeEngine 作为回归夹具，校验前后完全相等。

复用此前有效成片：bparrun_da55cbfea7b5cd1a，708 帧，8.12545 秒，exact，编码 1 次，中间视频 0，SSIM 0.985059；0914 (7) 回归 bparrun_a8ebeb51a2df4bd7 的 380 帧与原输出一致。本次只改后端交付校验，未重跑视频或 SSIM。
