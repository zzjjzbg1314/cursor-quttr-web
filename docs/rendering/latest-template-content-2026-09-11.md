# 模板仅使用最新正式内容

用户最新要求：每个模板只保留最新验收并发布成功的正式内容，不再长期保留历史回退。更新期间的临时输入与被项目、进行中任务引用的数据不能直接删除。

本次在 MusicMvTemplateCatalogService.publish 增加入口校验：存在不同当前内容时，目标顺序必须严格更新；旧内容、相同顺序的不同内容或缺失顺序明确拒绝。首次发布及重复提交当前内容仍走原有能力、运行包、场景和 SSIM 证据校验。

6 项聚焦测试通过，其中发布正例覆盖首次发布、更新替换、当前内容重复提交；负例覆盖旧内容、重复顺序、缺失顺序及验收场景不匹配；另验证原生曲速与非线性场景保存读取和缺失时钟阻断。

范围限制：这是服务入口检查，数据库并发切换的原子保护尚未实现。没有清理历史行或资源，没有移除版本表，没有修改项目绑定。运行中的 8080 尚未加载该改动；未 push、未运行渲染或 SSIM。整体单份正式内容策略尚未完成，不能以此宣称所有并发发布均已满足最新优先。

## 数据库提交阶段的新旧顺序保护

仓储 publish() 将新旧顺序判断放入更新当前内容指针的 SQL 条件中：目标属于同一模板且 browser_ready，目标必须比提交时的当前内容更新（或是当前内容的重复提交）。随后仅把当前指针选中的目标标记为 published，并在同批操作末尾读取实际当前内容；仓储返回是否选中目标，服务在失败时明确报告 TEMPLATE_PUBLICATION_SUPERSEDED，避免迟到请求返回发布成功。

8 项聚焦检查通过：6 项服务和原生场景检查，加 2 项通过 H2 内存数据库执行生产 SQL 的检查。覆盖较新请求先提交、较旧请求先提交两种顺序，旧请求迟到不能覆盖；目标缺失、不就绪、属于其他模板、模板已删除或当前内容已失去就绪状态时拒绝。H2 使用项目既有测试依赖，没有增加生产数据库依赖。

验证限制：上述顺序测试覆盖竞争请求先后落库的结果，不是云端 D1 的同时请求压力测试。没有执行线上发布，没有启动渲染或 SSIM。运行中 8080 仍未加载本轮改动。历史记录和无引用资源清理尚未实施，整个单份正式内容目标未完成。

## 旧素材清理按具体引用判定

已有 TemplateMediaCleanupService 对队列中的旧素材执行引用检查，但旧仓储查询把同一模板的任意项目/任务都计作引用，导致新内容上的项目也阻止旧素材清理。本次按具体内容绑定及显式素材引用判定：保留同一旧内容的项目和任务、内容绑定未知的项目、任意项目 draft_json 或任务 request/result/evidence 中引用该素材的记录，以及场景和媒体表直接引用。缺少旧内容标识或查询证据不足时仍保留。

14 项检查通过：6 项发布及原生场景、3 项实际 H2 SQL、5 项清理服务。新增 SQL 用例确认新内容绑定本身不误占用旧素材，各类直接引用和未知绑定阻断清理。清理服务测试仅使用模拟 provider，验证检查引用先于删除以及失败重试，没有实际删除云端素材。

范围：该改动作用于已有旧素材队列；尚未把整个旧内容记录及运行包加入自动淘汰，也未删除版本表记录。8080 未加载本轮改动，未运行渲染或 SSIM。单份正式内容策略仍未完成。

## 项目保存与旧内容淘汰的绑定保护

项目原先先验证再无条件 UPSERT 模板绑定，存在验证后旧内容被清理、仍写入失效标识的间隙。本次将绑定存在性放进 MusicMvProjectDraftRepository.saveSnapshot() 的 INSERT SELECT 条件：无模板选择时正常保存；新绑定只允许模板当前内容；已有项目可继续保存其原绑定，但目标内容必须仍存在且模板未删除。未满足条件时不写项目，后续照片关联更新继续受原有写入标记与修订号保护。

5 项项目保存检查通过，其中一个用例捕获生产生成的 SQL 和参数，通过 Python 标准库 SQLite 实际执行七种情形：当前内容、旧内容、已有旧绑定、目标不存在、无选择、不完整选择、已有绑定目标被删除。最后一种确认失败不更改旧项目也不删除照片关联。其余四项验证原项目服务的修订号、授权等行为。Python 语法检查通过；测试未增加生产依赖。

没有操作真实项目、渲染任务或云端数据库。没有删除历史记录，8080 尚未加载改动；未运行渲染或 SSIM。该保护是自动淘汰旧内容的前置条件，淘汰流程本身仍未接入。

## 无引用旧模板自动淘汰

网站后台既有素材清理任务现在先淘汰已被当前已发布、浏览器就绪内容替代的旧模板记录，每轮最多 20 条。数据库批处理先登记云媒体与最小运行包清理任务，再删除旧内容的模板子记录；保留当前内容和更新的待验收内容。项目绑定、项目快照、渲染任务及其证据中仍有引用，或素材/运行包尚未完成上传时均保留。项目未明确绑定内容（空值或空字符串）也保守保留。

云文件沿用至少 24 小时延迟和每轮最多 50 个的清理队列。运行包删除仅允许来源模板与内容目录下的 64 位哈希 ZIP；删除前复核模板媒体、场景、项目、任务、共享资源记录与用户素材 URL 引用。失败或无法确认引用时延后重试。用户照片表只有引用查询，没有更新或删除；共享 SDK 和共享资源文件也没有加入这次删除范围。

验证：Java 定向编译通过；旧内容淘汰使用生产生成 SQL 在 SQLite 执行 11 种场景并检查外键及用户素材记录；9 项清理服务测试使用模拟存储验证删除顺序、引用保留、错误重试与用户目录拒绝；3 项实际 SQL 发布/引用测试及 6 项发布服务/原生场景检查通过；Python 语法检查和 Git 差异检查通过。临时检查入口最初指向旧测试类或错误包名，修正并编译当前源码后全部通过。

本次没有启动渲染或 SSIM（不改变渲染规划/执行），没有重启网站服务，也没有执行真实云文件删除。代码接通不代表线上历史记录已经清理完成。后续启用前仍需核对实际候选数量和引用情况；仍被已有项目或任务使用的依赖不会为追求数量强制删除。


## 实际库只读核对

通过运行中网站后端的同一 D1 配置执行生产候选查询，只读核对得到：8 个已上架模板、22 条内容记录，其中 14 条被当前内容替代；当前清理候选为 0。14 条均受到同模板但未明确绑定内容的历史项目保护，其中 7 条另外有直接项目或渲染任务引用。没有修改项目、用户照片、模板数据或云文件，没有重启 8080。

进一步检查前端 `lib/project-template-version.ts`：没有明确内容标识的项目会选择模板当前内容；`project-template-local-fallback.ts` 对云端明确 null 不恢复本地旧身份。说明“没有明确绑定”不等于“已证明使用旧内容”，现有保护可能过宽。但尚需结合工作区加载及快照字段核对，不能仅凭这两个辅助函数删除已有依赖。本轮保留保护并记录真实清理数量为零，没有以代码已提交宣称存储已经收敛。

聚合证据：`latest-template-retirement-audit-2026-09-11.json`。不收录用户身份、照片地址或访问凭据。该结果是一次数据库快照，不代替执行清理前的再次引用检查。
