# 零槽位交付修复接入本地服务

已将上一轮通过定向检查的编译产物接入对应本地服务：研究端15695e66，网站后端f80415e。仅重启经端口、工作目录及Java主类核对的8082和8080进程；沿用原启动参数和环境，不输出凭据，不处理其他应用。已保留原class备份，13个替换class与已测试产物哈希一致。

8080 PID由89089变为17202，8082由71622变为17387；两者HTTP就绪。每次重启前后公开目录均为8个模板，templateId/currentVersionId集合一致。未改动任何发布指针。

实际8080接口边界验证：
- 缺失slots：400 VALIDATION_FAILED。
- slots=null：400 VALIDATION_FAILED。
- slots=[]：通过DTO槽位校验，随后409 TEMPLATE_VALIDATION_NOT_EXACT。

所有诊断请求均明确声明未验收、编码次数0；没有伪造通过证据。通过代码核对，最后一类请求在promote第一步requirePromotionEvidence即被拒绝，早于类别查询、模板仓储查询及写入。该验证说明显式零槽位已进入实际运行接口，同时未验收发布门禁保留；不等于已完成零槽位模板的真实云端上架往返。

本轮只接入已经验证过的相同代码与检查接口边界，没有新增渲染或SSIM，未push。此前484帧基础渲染结果仍按原报告范围使用。完整nativeEngine迁移、零槽位实际发布往返及上传性能仍未全部完成。
