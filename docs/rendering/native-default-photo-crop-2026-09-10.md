# 模板原图裁剪交付修复（2026-09-10）

换图链路检查发现，模板原图裁剪在前端提交和网站后端交付时分别丢失，后端幂等摘要也排除了原图裁剪，导致预览与导出可能不一致。

修复：前端projectRenderSlot对原图和替换图片统一携带裁剪，仅切换图片来源；后端MusicMvRenderJobService对两种来源均保留crop并计入任务指纹。原图仍从模板媒体交付，替换图仍走资源归属验证，没有改变图片权限。

验证：前端类型检查、5项针对性测试通过；后端Java8定向编译和25项服务测试通过，覆盖原图裁剪交付、原图裁剪改变时拒绝旧幂等请求及原有文字/渲染契约。

完整基础浏览器导出migration-default-photo-crop-export使用前端实际提交构造器和新0909全部原始场景，全部原图槽位裁剪为x75/y25/zoom1.5。1049帧、1080×1920、34.966667秒，exact，一次编码，零中间视频；渲染22.10093秒，总计24.65285秒。最终MP4音视频可解析，28248229字节。单worker、15分钟外部硬截止，监督器32.796秒退出。输出在/tmp/storyai-0909-new/migration-default-photo-crop-export/native-crop-edited-output.mp4，紧凑证据同目录events.jsonl。临时网站诊断文件已删除，未运行新SSIM，不覆盖原草稿验收。

验证边界：网站后端真实服务方法通过模拟资源提供者/数据库的测试验证；完整渲染用本地独立验证页，未宣称客户React页面经8080在线服务全部贯通。8080进程未为本次修改重启，正式生效需加载新构建。整体nativeEngine迁移仍未完成，后续继续换图宽高比、客户交互与其他场景覆盖。
