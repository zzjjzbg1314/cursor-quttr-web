# 待发布候选预览接口

内部同步鉴权接口 GET /internal/music-mv/v1/templates/{templateId}/versions/{versionId}/candidate-preview 复用客户版本场景组装：资源下载、照片槽位、输出规格均来自网站后端。候选必须 validated，场景 ready，参考 ready，且已通过 SSIM 的场景及参考哈希匹配。普通客户已发布版本接口仍拒绝该候选。此入口不更改当前版本，不执行发布。

Java 编译通过，模板服务 63 项定向测试通过。新增测试覆盖候选场景组装、保留原当前版本、普通客户接口拒绝候选、参考不匹配和 draft 状态阻断。

仅完成代码与定向测试。实际 0905 候选通过此接口的资源读取与浏览器导出尚待验证；项目保存、客户上传输出、回退也未完成，不能据此宣称完整客户链路完成。
