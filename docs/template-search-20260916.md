# 上线前模板检索改造验收

日期：2026-09-16

## 范围
统一列表和总数的关键词匹配；检索所有标题/描述翻译、标签、来源元数据及当前分类关联。11 个分类增加中英、西班牙语、葡萄牙语、印尼语常见别名。别名按完整查询匹配，不宣称任意句子的语义检索。排序保留运营顺序，增加模板 ID 稳定兜底。

新发布模板保存有效分类后自动参与别名检索；已有模板无需迁移。修改分类也立即使用当前关联。未发布模板仍被排除。未修改渲染契约、运行包和资源。

## 验证
- Python 测试夹具 py_compile 通过。
- TemplateSearchTest 2 项、MusicMvTemplateCatalogRepositoryTest 13 项、MusicMvTemplateCatalogServiceTest 86 项，共 101 项通过。
- SQLite 夹具执行实际生成的 SQL，验证新增模板、改分类、多语言标题、次分类、可见性及字面通配符。
- 本地网站代理连接现有目录：总数 46；wedding、婚礼、婚禮、boda、casamento、pernikahan 均返回相同 7 个模板。
- 婚礼关键词叠加 1～10 张照片返回 2 个；每页 2 条的前两页无重复，总数均为 7。
- 不存在的关键词和字面百分号返回 0；未向真实目录写入测试数据。
- 搜索链路不影响渲染规划或执行，因此未运行渲染及 SSIM。
