# 网站内置视频原字节交付修复

0913 (2) 发布为 tpl_f5f7481372b2e170 / tplver_4779a091c151468a81556adae4b35646 后，内置视频资源被签发为 Cloudflare Stream 的 HLS 主清单。原生媒体加载器把下载结果注册为 Blob；日志显示文件只有 1094 字节，insertClip2 在片段 EB3C4B56-2745-4A8E-AC33-DE6F19546629 返回 -1。带正常网站 Origin 获取同一 URL，确认 1094 字节内容以 #EXTM3U 开头。

新同步流程把内置 MP4 的原始字节独立存入内容寻址 R2，作为 sourceAsset 交付；不重新编码，不打入完整运行包。来源头部必须为 MP4 ftyp，HLS 清单被拒绝，原有 PNG/GIF 类型校验继续保留。

已有发布版本保持不可变。repair-video-resources 只注册原视频资产及版本引用，要求本地草稿身份、媒体角色、原始 SHA-256 和大小均与发布登记一致，不调用 browser-scene 写入，也不重建运行包。网站模板读取和导出会话按已发布媒体哈希找到登记的原视频；校验 ready、类型和大小后签发下载地址。未登记的旧媒体保留原交付行为。已发布场景不可变保护未放宽。

目标源文件为 277367 字节，SHA-256 为 39cd69ac0f5e40e0839686464ace9e34feee72a53023fc7c62ac50e18fa02ab8。修复后从网站 3000 的公开模板 API 读取真实交付合同；与修复前比较 scene 完全一致，14 个发布版本指针未变化。曾尝试的通用运行包修复被完整场景一致性保护阻止；场景引用更新被已发布不可变保护阻止；均未改写发布场景。最终采用上述只登记原视频方式成功。

验证：管理端 42 项、网站后端 125 项定向测试通过，覆盖原字节登记、错误格式拒绝、哈希/大小约束、旧交付兼容、导出会话读取和不可变场景保护。未改客户前端代码，不运行旧 Mac/Python 矩阵。

使用客户网站生产 createBrowserRuntimeSession 与 renderBrowserMusicMv，通过独立本地验证页加载网站返回的全部模板资源；音乐使用已保存基准音轨以固定比较输入。第 5 秒精确预览成功，695 帧完整导出成功，1080×1920/30fps，视频时长 23.166667 秒，含 AAC 容器时长 23.219955 秒。exact、编码 1 次、中间视频 0，渲染 19.21113 秒，总阶段耗时 20.132065 秒，OPFS 文件和 Writer 残留均为 0。一个验证任务，15 分钟硬截止。

随后将实际网站资源成片与已有原生基准比较：vpar_8a64f0675e6fee02，基础 7 点 SSIM 均值 0.977597、最低 0.945547，按原 0.9 门槛通过；额外 16 个压力点均值 0.854634、最低 0.13021，尾帧仍存在诊断差异，不宣称逐帧一致。比对不调整阈值、时间对齐或画面参数，15 分钟截止。详细指标见 validation.json；本机输出及报告在 /tmp/storyai-exact-video/。

仅本地服务加载新代码；未推送或部署远端服务。已向现有资产仓登记同源原视频，不修改模板照片、时间轴、效果、原播放地址或发布状态。
